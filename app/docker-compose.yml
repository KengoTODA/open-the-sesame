version: "3"
services:
  nginx:
    build:
      context: ./nginx
    ports:
      - "80:80"
    depends_on:
      - app
    env_file:
      - ../.env
    restart: unless-stopped
  app:
    build:
      context: .
    env_file:
      - ../.env
    restart: unless-stopped
